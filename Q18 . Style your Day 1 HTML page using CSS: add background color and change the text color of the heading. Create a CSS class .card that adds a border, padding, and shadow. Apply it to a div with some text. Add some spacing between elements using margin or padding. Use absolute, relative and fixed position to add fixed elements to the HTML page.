<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Styled HTML Page</title>
  <style>
    body {
      background-color: #f5f7fa;
      font-family: Arial, sans-serif;
      margin: 20px;
    }

    h1 {
      color: #2e86de;
    }

    p {
      margin-bottom: 20px;
    }

    img {
      max-width: 100%;
      height: auto;
      margin-bottom: 20px;
    }

    form {
      margin-top: 20px;
    }

    input, button {
      display: block;
      margin: 10px 0;
      padding: 8px;
      width: 100%;
      max-width: 300px;
    }

    .card {
      border: 1px solid #ccc;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      margin-top: 30px;
      background-color: #fff;
    }

    .fixed-box {
      position: fixed;
      top: 10px;
      right: 10px;
      background-color: #ff6b6b;
      color: white;
      padding: 10px;
      border-radius: 4px;
    }

    .relative-box {
      position: relative;
      background-color: #48dbfb;
      color: white;
      padding: 10px;
      margin-top: 30px;
    }

    .absolute-box {
      position: absolute;
      top: 20px;
      left: 20px;
      background-color: #1dd1a1;
      color: white;
      padding: 10px;
    }

    .wrapper {
      position: relative;
      height: 150px;
      background-color: #f0f0f0;
      margin-top: 40px;
    }
  </style>
</head>
<body>

  <div class="fixed-box">Fixed Notification</div>

  <h1>Welcome to My Website</h1>
  <p>This is a simple HTML5 page styled using CSS.</p>

  <img src="https://via.placeholder.com/300" alt="Placeholder Image">

  <form aria-label="Contact Form">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" required>

    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>

    <button type="submit">Submit</button>
  </form>

  <div class="card">
    This is a card styled with a border, padding, and shadow.
  </div>

  <div class="relative-box">
    This is a relative positioned box.
  </div>

  <div class="wrapper">
    <div class="absolute-box">
      This is an absolute positioned box inside a relative container.
    </div>
  </div>

</body>
</html>
